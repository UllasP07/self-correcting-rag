# data/

Drop your enterprise documents here (`.pdf`, `.md`, `.txt`, `.xlsx`). The
ingestion script (`python -m src.rag.ingest`) walks this folder recursively.

`acme_employee_handbook.md` is a sample so the thin slice works out of the box.
Real PDFs/Excel are git-ignored (see `.gitignore`) — add your own.
